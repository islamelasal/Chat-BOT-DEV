{
  "name": "project-agent-chrome-extension",
  "version": "1.6.0",
  "private": true,
  "type": "module",
  "description": "Static Chrome extension with a multi-provider free-tier agent engine.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "validate": "node scripts/validate.mjs"
  }
}
